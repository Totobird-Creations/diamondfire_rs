[package]
name        = "bridgecg_diamondfire"
version     = "0.1.0"
authors     = [ "Totobird Creations <https://github.com/Totobird-Creations>" ]
description = "Codegen/linker common datatypes and functions for the DiamondFire block language compiler target"

edition = "2024"

[features]
extern_names = [ "dep:bincode" ]
bridge_items = [ "dep:bincode", "dfmir" ]
dfmir        = [ "dep:bincode", "rustc_private" ]
dflir        = [ ]

rustc_private = [ ]

[package.metadata.rust-analyzer]
rustc_private = true

[lints.rust]
unused_parens = "allow"


[dependencies.bincode]
version  = "2.0"
optional = true
